require 'rails_helper'

describe "Private module methods" do

  it "Defines a private module method" do
    #TODO: Make this readable

    # def add_html_line_breaks(string, options={})
    #   line_width = options[:line_width] || 20
    #   simple_format(word_wrap(string, line_width: line_width))
    # end
    # private :add_html_line_breaks

  end

end
